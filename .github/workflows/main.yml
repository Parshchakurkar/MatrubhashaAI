name: Sync to Azure DevOps
on:
  push:
    branches: [ main ]

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Mirror to Azure DevOps
        run: |
          git config user.name "GitHub Sync Bot"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote add azure https://$ADO_PAT@dev.azure.com/parshchakurkar/MatrubhashaAI/_git/MatrubhashaAI
          git fetch --unshallow
          git push https://dev.azure.com/parshchakurkar/MatrubhashaAI/_git/MatrubhashaAI main
        env:
          ADO_PAT: ${{ secrets.ADO_PAT }}
