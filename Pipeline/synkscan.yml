# name: $(Build.DefinitionName)_$(Build.SourceBranchName)_$(date:yyyyMMdd)$(rev:.rrrr)

# trigger:
#   branches:
#     include:
#       - main

# pool:
#   vmImage: windows-latest

# variables:
  
# jobs:
  