apiVersion: apps/v1
kind: Deployment
metadata:
  name: matrubhashaai-deployment
  labels:
    app: matrubhashaai
  
spec:
  replicas : 1 # Default value is 1
  strategy: 
    type: RollingUpdate
  selector:
    matchLabels:
      app: matrubhashaai
  template:
    metadata:
      labels:
        app: matrubhashaai
    spec:
      containers:
      - name: matrubhashaai
        image: matrubhashaaiacr.azurecr.io/matrubhashaai:342
        ports:
        - containerPort: 5000
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        imagePullPolicy: IfNotPresent
        

